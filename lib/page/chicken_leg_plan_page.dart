import 'package:flutter/material.dart';

class ChickenLegPlanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("晚上加个🍗?")),
        body: Image.asset("res/images/ic_alipay.jpg"));
  }
}
